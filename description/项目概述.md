# Yu-Agent 项目概述

## 📌 项目基本信息

**项目名称**: yu_agent
**版本**: 0.1.2
**作者**: Zhuoyang Wu
**许可证**: MIT
**Python要求**: >= 3.10
**最后更新**: 2025年2月

---

## 🎯 项目概述

**yu_agent** 是一个基于《Hello Agents》教科书设计模式的智能体（Agent）框架实现，为多个LLM提供商提供统一接口，并实现了四种核心Agent推理模式。

### 核心理念
- **简洁性配合可扩展性** - 每个组件都可独立使用
- **清晰度优先** - 代码清晰易懂，而不是过度抽象
- **教学友好** - 适合学习和理解Agent框架的工作原理

### 项目规模
- **源代码**: 44个Python文件，~10,000+行代码
- **最大模块**: RAG Pipeline（1206行）、Semantic Memory（1190行）
- **测试覆盖**: 记忆、RAG、Agent、上下文构建等核心功能
- **文档完整**: 代码注释、类型提示、使用示例

---

## 🏗️ 项目架构

```
┌─────────────────────────────────────────────────────────────┐
│                     yu_agent 框架                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│  │   Simple     │  │   ReAct      │  │ Reflection   │    │
│  │   Agent      │  │   Agent      │  │   Agent      │    │
│  └──────────────┘  └──────────────┘  └──────────────┘    │
│  ┌──────────────┐                                         │
│  │ PlanAndSolve │                                         │
│  │   Agent      │                                         │
│  └──────────────┘                                         │
│         │                                                │
│         └─────────────────┬──────────────┬───────────────┤
│                           │              │               │
│  ┌─────────────────┐ ┌────▼──────┐ ┌────▼──────┐       │
│  │  工具系统        │ │  LLM 接口  │ │  记忆系统  │       │
│  │ (8+内置工具)    │ │ (8提供商)   │ │ (4层记忆)  │       │
│  └─────────────────┘ └───────────┘ └───────────┘       │
│         │                                                │
│         └──────────────────┬──────────────────────────────┤
│                            │                             │
│                   ┌────────▼────────┐                   │
│                   │  上下文构建器     │                   │
│                   │  (GSSC流程)     │                   │
│                   └────────────────┘                    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🔑 核心功能模块

### 1. Agent推理模式 (`agents/`)

#### SimpleAgent - 基础对话
```python
from yu_agent import SimpleAgent, AgentsLLM

agent = SimpleAgent("聊天助手", AgentsLLM())
response = agent.run("你好！")
```
- 适用场景：简单问答、聊天机器人
- 特点：直接LLM调用，保留历史记录

#### ReActAgent - 推理+行动
```python
from yu_agent import ReActAgent, global_registry, AgentsLLM

agent = ReActAgent("问题求解器", AgentsLLM(), global_registry)
response = agent.run("计算2**10 + 15等于多少？")
```
- 适用场景：需要工具调用的复杂问题
- 特点：迭代思考→行动→观察循环，支持函数调用

#### ReflectionAgent - 自我反思
```python
from yu_agent import ReflectionAgent, AgentsLLM

agent = ReflectionAgent("代码生成器", AgentsLLM())
response = agent.run("编写一个快速排序算法")
```
- 适用场景：代码生成、文档编写等需要质量保证的任务
- 特点：执行→反思→优化循环，自动改进质量

#### PlanAndSolveAgent - 分层分解
```python
from yu_agent import PlanAndSolveAgent, AgentsLLM

agent = PlanAndSolveAgent("数学问题求解", AgentsLLM())
response = agent.run("小明有3个苹果，买了2个，吃了1个，还有多少个？")
```
- 适用场景：多步骤数学问题、复杂推理
- 特点：规划阶段→执行阶段，逐步分解复杂问题

---

### 2. LLM统一接口 (`core/llm.py`)

支持8+个LLM提供商的自动检测和统一调用：

```python
from yu_agent import AgentsLLM

# 自动检测配置的提供商
llm = AgentsLLM()

# 流式调用
for chunk in llm.think(messages):
    print(chunk, end="", flush=True)

# 非流式调用
response = llm.invoke(messages)
```

**支持的提供商**：
- OpenAI (ChatGPT, GPT-4)
- DeepSeek
- 阿里Qwen (DashScope)
- ModelScope
- Kimi (Moonshot)
- Zhipu (GLM)
- Ollama (本地部署)
- vLLM (开源推理)

**自动检测机制**：
1. 检查提供商特定的环境变量
2. 分析API密钥格式
3. 检查base_url模式
4. 回退到通用变量或默认值

---

### 3. 工具系统 (`tools/`)

#### 内置工具（8个）

**计算器** - 数学运算
```python
from yu_agent import CalculatorTool, global_registry

# 使用全局注册表
result = global_registry.execute_tool("calculator", "2**10 + sqrt(16)")
# 输出: 1028.0
```
- 运算符：+, -, *, /, **, ^
- 函数：abs, round, max, min, sum, sqrt, sin, cos, tan, log, exp
- 常数：pi, e
- 实现：基于AST的安全求值（无eval风险）

**搜索工具** - 网络搜索
```python
from yu_agent import SearchTool, global_registry

result = global_registry.execute_tool("search", "Python最新版本特性")
# 返回: 搜索结果 + 3个来源
```
- Tavily API（首选）：AI优化的搜索结果
- SerpAPI（备选）：Google搜索
- 自动回退机制

**RAG工具** - 检索增强生成
```python
from yu_agent import RAGTool

rag = RAGTool(knowledge_base_path="./kb")

# 添加文档
rag.run({"action": "add", "documents": ["文档内容"], "metadata": {}})

# 搜索
results = rag.run({"action": "search", "query": "查询内容", "top_k": 5})
```
- 支持格式：PDF、Markdown、纯文本
- 智能分块：基于段落和标题的语义感知
- 向量检索：Qdrant向量数据库
- 知识图谱：Neo4j图检索
- 结果排序：向量分数 + 图信号融合

**记忆工具** - 记忆系统集成
```python
from yu_agent.tools.builtin import MemoryTool

memory = MemoryTool(user_id="user123")

# 添加记忆
memory.run({
    "action": "add",
    "content": "用户喜欢Python编程",
    "memory_type": "semantic",
    "importance": 0.8
})

# 搜索记忆
results = memory.execute("search", query="用户编程偏好")
```

**笔记工具** - 笔记管理
```python
from yu_agent.tools.builtin import NoteTool

note_tool = NoteTool()

# 创建笔记
note_tool.run({"action": "create", "title": "学习笔记", "content": "..."})

# 搜索笔记
results = note_tool.run({"action": "search", "query": "关键词"})
```

**终端工具** - 命令执行
```python
from yu_agent.tools.builtin import TerminalTool

terminal = TerminalTool()

# 执行命令
output = terminal.run({"action": "execute", "command": "python --version"})
```

#### 工具注册表

```python
from yu_agent import ToolRegistry, global_registry

# 使用全局注册表
result = global_registry.execute_tool("calculator", "2+2")

# 或创建自己的注册表
registry = ToolRegistry()
registry.register_tool(MyCustomTool())
tools_list = registry.list_tools()  # 列出所有工具
```

#### 工具链式执行

```python
from yu_agent import ToolChain

chain = ToolChain()
chain.add_step("search", "搜索关于{topic}的信息", "search_result")
chain.add_step("calculator", "根据{search_result}计算{equation}", "result")

result = chain.execute(registry, {"topic": "AI", "equation": "2+2"})
```

#### 异步工具执行

```python
import asyncio
from yu_agent import AsyncToolExecutor

async def main():
    async with AsyncToolExecutor(registry) as executor:
        # 并行执行多个工具
        results = await executor.execute_tools_parallel([
            ("calculator", "2+2"),
            ("search", "AI新闻"),
            ("calculator", "10*5")
        ])

asyncio.run(main())
```

---

### 4. 记忆系统 (`memory/`)

四层分层记忆架构，支持不同时效性和存储方式：

#### 工作记忆 (WorkingMemory)
- **时效**：短期（分钟）
- **存储**：内存（堆队列）
- **容量**：10-100项
- **用途**：会话上下文、临时信息
- **检索**：TF-IDF + 时间衰减

```python
from yu_agent.memory import MemoryManager, MemoryConfig

config = MemoryConfig(working_memory_capacity=10)
manager = MemoryManager(config, user_id="user123")

# 添加工作记忆
manager.add_memory("当前讨论的话题是Python", memory_type="working")

# 获取上下文摘要
summary = manager.memory_types["working"].get_context_summary()
```

#### 情景记忆 (EpisodicMemory)
- **时效**：中期（天周）
- **存储**：SQLite + Qdrant向量
- **用途**：用户交互历史、事件记录
- **检索**：向量相似度 + 时间排序

```python
# 添加情景记忆
manager.add_memory(
    content="用户今天完成了数据分析任务",
    memory_type="episodic",
    importance=0.8,
    metadata={"task_type": "数据分析", "outcome": "成功"}
)

# 按会话检索
session_episodes = manager.memory_types["episodic"].get_session_episodes("session_001")

# 发现行为模式
patterns = manager.memory_types["episodic"].find_patterns(user_id="user123")
```

#### 语义记忆 (SemanticMemory)
- **时效**：长期（永久）
- **存储**：Neo4j知识图谱 + Qdrant向量
- **用途**：知识库、概念、规则
- **检索**：向量相似度 + 图遍历

```python
# 添加语义记忆
manager.add_memory(
    content="Python是一种高级编程语言，具有简洁的语法",
    memory_type="semantic",
    importance=0.9,
    metadata={"tags": ["编程", "Python"], "category": "编程语言"}
)

# 搜索知识
results = manager.memory_types["semantic"].retrieve("编程语言特性", limit=10)

# 查看知识图谱
kg = manager.memory_types["semantic"].export_knowledge_graph()
```

#### 感知记忆 (PerceptualMemory)
- **时效**：短期
- **存储**：灵活（图片、音频、文本）
- **用途**：多模态感知输入
- **检索**：灵活

```python
# 添加感知记忆
manager.add_memory(
    content="用户上传的图表展示了销售增长趋势",
    memory_type="perceptual",
    metadata={"modality": "image", "description": "销售图表"}
)
```

#### 记忆操作

```python
from yu_agent.memory import MemoryManager, MemoryConfig

manager = MemoryManager(MemoryConfig(), user_id="user123")

# 添加记忆
manager.add_memory(content="...", memory_type="semantic", importance=0.8)

# 检索记忆
results = manager.retrieve_memories(
    query="用户偏好",
    memory_types=["semantic", "episodic"],
    limit=5,
    min_importance=0.5
)

# 遗忘机制
manager.forget_memories(strategy="importance_based", threshold=0.2)

# 记忆整合（从working → episodic）
manager.consolidate_memories(from_type="working", to_type="episodic")

# 获取统计
stats = manager.get_memory_stats()
```

---

### 5. RAG完整流程 (`memory/rag/`)

从文档加载到结果返回的完整RAG管道：

```python
from yu_agent.memory.rag import RAG Pipeline, DocumentProcessor

# 初始化RAG Pipeline
pipeline = RAGPipeline(
    qdrant_url="http://localhost:6333",
    collection_name="knowledge_base",
    embedding_type="local"
)

# 1. 加载文档
processor = DocumentProcessor()
documents = processor.load_documents([
    "file1.pdf",
    "file2.md",
    "file3.txt"
])

# 2. 分块处理
chunks = processor.chunk_documents(documents)

# 3. 嵌入并存储
pipeline.add_documents(chunks)

# 4. 检索和排序
results = pipeline.retrieve(
    query="如何优化内存占用",
    top_k=5,
    use_graph=True  # 使用知识图谱
)

# 5. 处理结果
for doc in results:
    print(f"标题: {doc.metadata['source']}")
    print(f"内容: {doc.content}")
```

**RAG特性**：
- **多格式支持**：PDF、Markdown、纯文本
- **智能分块**：基于段落和标题的语义感知
- **混合检索**：向量相似度 + 知识图谱
- **结果排序**：融合多个信号（向量分数、图信号、频率）
- **上下文合并**：自动合并相邻文本块以改进上下文窗口

---

### 6. 上下文构建 (`context/`)

GSSC流程：Gather→Select→Structure→Compress

```python
from yu_agent.context import ContextBuilder, ContextConfig, ContextPacket
from yu_agent.tools.builtin import MemoryTool, RAGTool

# 初始化
memory_tool = MemoryTool(user_id="user123")
rag_tool = RAGTool(knowledge_base_path="./kb")

config = ContextConfig(
    max_tokens=4000,
    reserve_ratio=0.2,  # 预留20%给模型输出
    min_relevance=0.2,
    enable_compression=True
)

builder = ContextBuilder(
    memory_tool=memory_tool,
    rag_tool=rag_tool,
    config=config
)

# 构建优化后的上下文
context = builder.build(
    user_query="如何优化Pandas的内存占用?",
    conversation_history=[...],
    system_instructions="你是一位资深的Python数据工程顾问。",
    additional_packets=[...]  # 可选：添加自定义内容
)

# 输出格式：
# [Role & Policies] - 系统指令和角色
# [Task] - 用户查询
# [Evidence] - 知识库和记忆的相关信息
# [Context] - 对话历史
# [Output] - 期望的输出格式
```

**ContextBuilder特点**：
- **智能收集**：从系统指令、记忆、RAG、对话历史收集信息
- **优先级筛选**：按相关性、新近性、优先级排序
- **结构化输出**：标准化的提示词格式
- **Token管理**：自动计数和压缩以保证不超出预算

---

## 🚀 快速开始

### 安装

```bash
# 基础安装
pip install -e .

# 完整安装（包含所有可选依赖）
pip install -e ".[memory,search,dev]"

# 或手动安装依赖
pip install -r requirements.txt
```

### 配置环境变量

创建 `.env` 文件在项目根目录：

```bash
# LLM配置
LLM_MODEL_ID=gpt-4o-mini
LLM_API_KEY=sk-...
LLM_BASE_URL=https://api.openai.com/v1
LLM_TIMEOUT=60

# 搜索配置（可选）
TAVILY_API_KEY=tvly-...
SERPAPI_API_KEY=...

# 记忆系统配置（可选）
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=...
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=password

# 嵌入模型配置（可选）
EMBED_MODEL_TYPE=local
EMBED_MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2
```

### 最简单的示例

```python
from yu_agent import SimpleAgent, AgentsLLM

# 创建Agent
agent = SimpleAgent("聊天机器人", AgentsLLM())

# 运行
response = agent.run("你好，请介绍一下自己")
print(response)
```

### 工具调用示例

```python
from yu_agent import ReActAgent, global_registry, AgentsLLM

# 使用全局注册表（包含所有内置工具）
agent = ReActAgent("计算助手", AgentsLLM(), global_registry)

# 运行（会自动调用calculator工具）
response = agent.run("计算 (2**10 + sqrt(100)) / 3 的结果")
print(response)
```

### 记忆系统示例

```python
from yu_agent.memory import MemoryManager, MemoryConfig

# 初始化
config = MemoryConfig(storage_path="./memory_data")
manager = MemoryManager(config, user_id="user_123")

# 添加记忆
manager.add_memory(
    content="用户喜欢数据分析和Python编程",
    memory_type="semantic",
    importance=0.8
)

# 检索
results = manager.retrieve_memories(
    query="用户的技术偏好",
    limit=5
)

print(results)
```

### RAG示例

```python
from yu_agent import RAGTool

# 初始化
rag = RAGTool(knowledge_base_path="./kb")

# 添加文档
rag.run({
    "action": "add",
    "documents": ["Python是一种编程语言...", "..."],
})

# 搜索
results = rag.run({
    "action": "search",
    "query": "Python特性",
    "top_k": 5
})

print(results)
```

### 集成示例（Agent + 记忆 + RAG + 上下文）

```python
from yu_agent import SimpleAgent, AgentsLLM
from yu_agent.context import ContextBuilder, ContextConfig
from yu_agent.tools.builtin import MemoryTool, RAGTool

class ContextAwareAgent(SimpleAgent):
    def __init__(self, name, llm, **kwargs):
        super().__init__(name=name, llm=llm)
        self.memory_tool = MemoryTool(user_id=kwargs.get("user_id", "default"))
        self.rag_tool = RAGTool(knowledge_base_path=kwargs.get("kb_path", "./kb"))

        self.context_builder = ContextBuilder(
            memory_tool=self.memory_tool,
            rag_tool=self.rag_tool,
            config=ContextConfig(max_tokens=4000)
        )
        self.conversation_history = []

    def run(self, user_input):
        # 构建优化上下文
        context = self.context_builder.build(
            user_query=user_input,
            conversation_history=self.conversation_history,
            system_instructions=self.system_prompt
        )

        # 调用LLM
        messages = [{"role": "user", "content": context}]
        response = self.llm.invoke(messages)

        # 更新历史和记忆
        self.conversation_history.append({"role": "user", "content": user_input})
        self.conversation_history.append({"role": "assistant", "content": response})

        return response

# 使用
agent = ContextAwareAgent(
    name="数据分析顾问",
    llm=AgentsLLM(),
    user_id="user123",
    kb_path="./data_science_kb"
)

response = agent.run("如何优化Pandas的内存占用?")
print(response)
```

---

## 📊 项目文件结构

```
yu_agent/
├── src/yu_agent/
│   ├── core/                    # 核心组件
│   │   ├── message.py          # 消息数据模型
│   │   ├── agent.py            # Agent基类
│   │   ├── llm.py              # LLM统一接口（8+提供商）
│   │   ├── config.py           # 配置管理
│   │   ├── exceptions.py       # 异常类定义
│   │   └── database_config.py  # 数据库配置
│   │
│   ├── agents/                 # Agent实现
│   │   ├── simple_agent.py     # 基础对话
│   │   ├── react_agent.py      # 推理+行动
│   │   ├── reflection_agent.py # 自我反思
│   │   └── plan_solve_agent.py # 分层分解
│   │
│   ├── tools/                  # 工具系统
│   │   ├── base.py             # Tool基类
│   │   ├── registry.py         # 工具注册表
│   │   ├── chain.py            # 工具链式执行
│   │   ├── async_executor.py   # 异步执行
│   │   └── builtin/            # 内置工具
│   │       ├── calculator.py   # 计算器
│   │       ├── search.py       # 网络搜索
│   │       ├── rag_tool.py     # RAG工具
│   │       ├── memory_tool.py  # 记忆工具
│   │       ├── note_tool.py    # 笔记工具
│   │       └── terminal_tool.py # 终端工具
│   │
│   ├── memory/                 # 记忆系统
│   │   ├── base.py             # 基础类
│   │   ├── manager.py          # 记忆管理器
│   │   ├── embedding.py        # 嵌入模型
│   │   ├── types/              # 4种记忆类型
│   │   │   ├── working.py
│   │   │   ├── episodic.py
│   │   │   ├── semantic.py
│   │   │   └── perceptual.py
│   │   ├── storage/            # 存储后端
│   │   │   ├── document_store.py
│   │   │   ├── qdrant_store.py
│   │   │   └── neo4j_store.py
│   │   └── rag/                # RAG系统
│   │       ├── document.py
│   │       └── pipeline.py
│   │
│   ├── context/                # 上下文构建
│   │   └── builder.py          # GSSC流程实现
│   │
│   ├── __init__.py             # 包导出（56项）
│   └── version.py              # 版本信息
│
├── tests/
│   ├── test_the_yu_agent/      # Agent和记忆测试
│   │   ├── test_memory.py
│   │   ├── test_rag.py
│   │   └── [测试文档]
│   │
│   └── test_context/           # 上下文构建测试
│       ├── test.py             # ContextBuilder集成测试
│       └── combine_with_agent.py # Agent集成测试
│
├── setup.py                    # 包配置
├── pyproject.toml              # 项目配置
├── requirements.txt            # 依赖列表
├── .env                        # 环境变量（示例）
├── readme.md                   # 项目文档
├── claude.md                   # 开发规范
├── description/                # 项目描述
│   ├── 项目概述.md
│   ├── 使用指南.md
│   └── 架构设计.md
└── [其他目录]
    ├── memory_data/            # 记忆存储
    ├── knowledge_base/         # RAG知识库
    └── learn_PDF/              # 学习文档
```

---

## 🔧 开发工具和配置

### 推荐开发环境

**IDE**: Visual Studio Code
- 已配置：`.vscode/` 目录包含设置
- 推荐插件：Python, Pylance, Black, Ruff

**代码质量工具**：
- **Black**: 代码格式化
- **Ruff**: 快速Linter
- **mypy**: 类型检查
- **pytest**: 单元测试

### 设置开发环境

```bash
# Linux/Mac
bash setup_dev_env.sh

# Windows PowerShell
.\setup_dev_env.ps1
```

### 常用命令

```bash
# 运行测试
pytest tests/

# 格式化代码
black src/

# 检查类型
mypy src/

# 运行linter
ruff check src/
```

---

## 📚 学习路径

### 初学者
1. 阅读 `简单对话` 示例，理解 SimpleAgent
2. 学习 `工具调用` 示例，理解工具系统
3. 研究 `core/llm.py`，理解LLM接口

### 进阶学习
1. 研究 `ReActAgent`，理解迭代思考和行动
2. 学习 `reflection_agent.py`，理解自我优化
3. 理解 `plan_solve_agent.py`，学习问题分解

### 高级应用
1. 深入 `memory/` 模块，掌握4层记忆架构
2. 研究 `memory/rag/pipeline.py`，理解完整RAG
3. 学习 `context/builder.py`，实现上下文优化

### 实战项目
1. 构建智能客服系统（SimpleAgent + 记忆）
2. 实现数据分析助手（ReActAgent + RAG）
3. 开发知识库系统（完整记忆系统）

---

## 🎓 核心概念速查表

| 概念 | 说明 | 主要文件 |
|------|------|---------|
| **Agent** | 智能体基类，定义run()接口 | core/agent.py |
| **SimpleAgent** | 基础对话Agent | agents/simple_agent.py |
| **ReActAgent** | 推理+行动Agent | agents/react_agent.py |
| **ReflectionAgent** | 自我反思Agent | agents/reflection_agent.py |
| **PlanAndSolveAgent** | 分层分解Agent | agents/plan_solve_agent.py |
| **AgentsLLM** | LLM统一客户端 | core/llm.py |
| **ToolRegistry** | 工具注册表 | tools/registry.py |
| **MemoryManager** | 记忆系统管理 | memory/manager.py |
| **WorkingMemory** | 短期会话记忆 | memory/types/working.py |
| **EpisodicMemory** | 中期交互历史 | memory/types/episodic.py |
| **SemanticMemory** | 长期知识库 | memory/types/semantic.py |
| **PerceptualMemory** | 多模态感知 | memory/types/perceptual.py |
| **RAGPipeline** | 完整RAG流程 | memory/rag/pipeline.py |
| **ContextBuilder** | 上下文构建器 | context/builder.py |

---

## 📞 获取帮助

### 常见问题

**Q: 如何修改默认LLM提供商？**
A: 修改环境变量 `LLM_MODEL_ID` 和相关API密钥，系统会自动检测。

**Q: 如何添加自定义工具？**
A: 继承 `Tool` 类，实现 `run()` 和 `get_parameters()` 方法，然后注册到 `ToolRegistry`。

**Q: 记忆系统需要什么依赖？**
A: 需要 Qdrant 和 Neo4j 服务运行。使用 Docker 快速启动：
```bash
docker run -p 6333:6333 qdrant/qdrant:latest
docker run -p 7687:7687 -e NEO4J_AUTH=neo4j/password neo4j:latest
```

**Q: 如何优化上下文质量？**
A: 使用 `ContextBuilder` 的 `ContextConfig` 调整参数：
- 增加 `max_tokens` 以包含更多信息
- 调整 `min_relevance` 阈值
- 设置 `reserve_ratio` 为模型输出预留空间

### 文档和资源

- **项目主文档**: `readme.md`
- **开发规范**: `claude.md`
- **测试文档**: `tests/` 目录下的 `*.md` 文件
- **学习资料**: `learn_PDF/` 目录

---

## 📝 更新日志

### v0.1.2 (2025-02)
- ✨ 新增ContextBuilder（GSSC上下文构建流程）
- 🐛 修复嵌入模型初始化问题
- 🔧 优化记忆系统搜索结果处理
- 📚 完善项目文档

### v0.1.1
- 🎉 完整的记忆系统实现（4层记忆）
- 📦 RAG系统集成
- 🛠️ 扩展工具系统

### v0.1.0 (初始版本)
- 🚀 4种Agent推理模式
- 🔗 统一LLM接口（8+提供商）
- 🛠️ 工具系统基础

---

## 📄 许可证

本项目采用 **MIT 许可证**。详见项目根目录的 `LICENSE` 文件。

---

**最后更新**: 2025年2月
**作者**: Zhuoyang Wu
**维护状态**: 积极维护中
