# YU_AGENT 依赖列表
# 由pyproject.toml定义，此文件用于快速参考

# 核心依赖 (必需)
openai==2.18.0
pydantic==2.12.5
python-dotenv>=1.0.0
requests>=2.31.0
tiktoken>=0.5.0

# 搜索工具
tavily>=0.7
serpapi>=0.1.5

# 数值计算
numpy>=1.26.0

# 向量和图数据库 (内存系统)
qdrant-client>=1.16.2
neo4j>=5.13.0

# 文本向量化和NLP
sentence-transformers>=2.2.0
dashscope>=1.0.0
spacy>=3.7.0

# MCP协议
fastmcp>=0.1.0

# 可选工具
selenium>=4.10.0
webdriver-manager>=4.0.0

# 开发依赖 (可选)
pytest>=7.0
pytest-asyncio>=0.21.0
black>=23.0
ruff>=0.1.0
mypy>=1.0

# 注意: 使用 pip install -e . 安装核心依赖
#      使用 pip install -e ".[memory]" 安装所有memory相关依赖
#      使用 pip install -e ".[dev]" 安装开发工具
